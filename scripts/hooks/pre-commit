#!/bin/sh
# Régénère TREE.md et le stage dans le commit en cours.
# Installé depuis scripts/hooks/pre-commit via scripts/lib/install-hooks.ps1.
root=$(git rev-parse --show-toplevel)
pwsh -NoLogo -NonInteractive -File "$root/scripts/hooks/update-tree.ps1" || exit 1
git add "$root/TREE.md"
