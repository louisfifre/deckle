{
    // Deckle launch profiles. Select from the Run and Debug panel (Ctrl+Shift+D),
    // then F5. Uses the PowerShell extension (ms-vscode.powershell).
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Deckle scripts",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/scripts/deckle.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}
