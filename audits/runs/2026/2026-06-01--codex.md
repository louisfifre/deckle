---
date: 2026-06-01
agent: codex
scope: weekly-project-audit
baseline: current-repository-state
commit: 36bbb84
status: watch
---

# Audit — 2026-06-01 — Codex

## Summary

Status: watch.
First Codex audit run; `HEAD` is one local commit ahead of `origin/main` and only touches `scripts/`, `.gitattributes`, and generated `TREE.md`. Release app build succeeded on repeat with `--no-restore`, but test health is not fully green in this environment.

## Security

Status: stable.
No newly changed sensitive surface was found in the local delta: hotkeys, clipboard, autostart, telemetry, and Ollama paths were untouched. `SECURITY.md` still matches the observed repository posture.

## Architecture

Status: stable.
The ahead commit is maintenance-only and does not move code across module boundaries or add host/business coupling. The `TREE.md` merge strategy is isolated to tooling and does not affect shipping assemblies.

## Testing

Status: watch.
`dotnet test tests/Deckle.Tests/Deckle.Tests.csproj -c Release -p:Platform=x64 --no-restore` ran 51 tests: 48 passed and 3 failed in `DispatcherQueueExtensionsTests` with `REGDB_E_CLASSNOTREG` on `DispatcherQueueController.CreateOnDedicatedThread()`. Full restore-backed validation was blocked here because this sandbox cannot read `%AppData%\NuGet\NuGet.Config`.

## Observability

Status: stable.
Observability coverage remains present in the suite (`Diagnostics`, `Hud`, `Lighting`, `Vision`, plus the Shell regression tests that failed at WinAppSDK activation rather than on event assertions). No changed file in the local delta weakens the EventSource or JSONL pipeline.

## UX Native Fit

Status: stable.
No UI or WinUI surface changed in the reviewed delta. The repeated Release app build completed successfully, so there is no fresh native-fit signal from XAML compilation beyond routine build hygiene.

## Documentation Drift

Status: stable.
The script change makes `TREE.md` deterministic and documents the `merge=ours` driver in both `.gitattributes` and `scripts/install-hooks.ps1`; that matches the generated file now checked in. No audit doctrine or security doc drift was observed.

## Follow-ups

Status: watch.
- Re-run the Shell dispatcher-queue tests on the maintainer workstation and confirm why `WindowsAppSdkBootstrap` is not sufficient in this runner.
- If the one-off `Deckle.Catalog` `XamlCompiler.exe` failure reappears, capture a binlog per ADR-0012 before treating it as a real regression.
