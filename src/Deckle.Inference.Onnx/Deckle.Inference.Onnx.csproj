<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Deckle.Inference.Onnx</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- SileroSpeechTimestamps (the pure state machine) is internal; the test
         project reaches it through the SDK InternalsVisibleTo item. -->
    <InternalsVisibleTo Include="Deckle.Inference.Onnx.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.ML.OnnxRuntime" />
  </ItemGroup>
</Project>
