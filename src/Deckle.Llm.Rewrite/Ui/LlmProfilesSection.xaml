<UserControl
    x:Class="Deckle.Llm.Rewrite.LlmProfilesSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:vm="using:Deckle.Llm.Rewrite"
    x:Name="Root">

    <UserControl.Resources>
        <vm:CtxTooltipConverter x:Key="CtxTooltipConverter" />
        <vm:TempTooltipConverter x:Key="TempTooltipConverter" />
    </UserControl.Resources>

    <StackPanel Tag="LlmProfilesSection"
                Spacing="{StaticResource SettingsCardSpacing}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Uid="LlmProfilesHeader"
                       Grid.Column="0"
                       Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />
            <HyperlinkButton x:Uid="Settings_SectionResetLink"
                             Grid.Column="1"
                             VerticalAlignment="Bottom"
                             Margin="0,0,0,2"
                             Click="ResetSection_Click" />
        </Grid>

        <TextBlock x:Uid="LlmProfilesBody"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="1,0,0,8" />

        <ItemsRepeater ItemsSource="{x:Bind Profiles}">
            <ItemsRepeater.Layout>
                <StackLayout Spacing="4" />
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="vm:ProfileViewModel">
                    <controls:SettingsExpander
                        Header="{x:Bind Name, Mode=OneWay}"
                        Description="{x:Bind Summary, Mode=OneWay}"
                        IsExpanded="{x:Bind IsNew, Mode=OneWay}">

                        <!-- Delete button in the header area -->
                        <Button Tag="{x:Bind}"
                                Click="DeleteProfile_Click">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="{StaticResource Icon.Delete}" FontSize="14" />
                                <TextBlock x:Uid="LlmProfile_RemoveLabel" />
                            </StackPanel>
                        </Button>

                        <controls:SettingsExpander.Items>
                            <controls:SettingsCard ContentAlignment="Vertical"
                                                   HorizontalContentAlignment="Stretch">
                                <StackPanel Spacing="12"
                                            HorizontalAlignment="Stretch">
                                    <TextBox x:Uid="LlmProfile_NameBox"
                                             Text="{x:Bind Name, Mode=TwoWay}"
                                             Tag="{x:Bind}"
                                             TextChanged="NameBox_TextChanged"
                                             HorizontalAlignment="Stretch" />

                                    <AutoSuggestBox x:Uid="LlmProfile_ModelBox"
                                                    Tag="{x:Bind}"
                                                    Text="{x:Bind Model, Mode=OneWay}"
                                                    GotFocus="ModelSuggest_GotFocus"
                                                    TextChanged="ModelSuggest_TextChanged"
                                                    SuggestionChosen="ModelSuggest_SuggestionChosen"
                                                    QuerySubmitted="ModelSuggest_QuerySubmitted"
                                                    HorizontalAlignment="Stretch" />

                                    <TextBox x:Uid="LlmProfile_SystemPromptBox"
                                             Text="{x:Bind SystemPrompt, Mode=TwoWay}"
                                             Tag="{x:Bind}"
                                             TextChanged="SystemPromptBox_TextChanged"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             MinHeight="100"
                                             MaxHeight="300"
                                             HorizontalAlignment="Stretch" />

                                    <TextBlock x:Uid="LlmProfile_SystemPromptBody"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,-4,0,0" />

                                    <!-- Temperature -->
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="12"
                                                VerticalAlignment="Bottom">
                                        <Slider x:Uid="LlmProfile_TemperatureSlider"
                                                Minimum="0" Maximum="2"
                                                StepFrequency="0.05"
                                                SmallChange="0.05"
                                                LargeChange="0.1"
                                                MinWidth="280"
                                                Value="{x:Bind Temperature, Mode=TwoWay}"
                                                ThumbToolTipValueConverter="{StaticResource TempTooltipConverter}" />
                                        <TextBlock Text="{x:Bind TempDisplay, Mode=OneWay}"
                                                   VerticalAlignment="Center"
                                                   MinWidth="40"
                                                   Style="{StaticResource BodyTextBlockStyle}" />
                                    </StackPanel>

                                    <TextBlock x:Uid="LlmProfile_TemperatureBody"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,-4,0,0" />

                                    <!-- Context size -->
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="12"
                                                VerticalAlignment="Bottom">
                                        <Slider x:Uid="LlmProfile_ContextSlider"
                                                Minimum="0" Maximum="8"
                                                StepFrequency="1"
                                                SnapsTo="StepValues"
                                                MinWidth="280"
                                                Value="{x:Bind CtxIndex, Mode=TwoWay}"
                                                ThumbToolTipValueConverter="{StaticResource CtxTooltipConverter}" />
                                        <TextBlock Text="{x:Bind CtxDisplay, Mode=OneWay}"
                                                   VerticalAlignment="Center"
                                                   MinWidth="40"
                                                   Style="{StaticResource BodyTextBlockStyle}" />
                                    </StackPanel>

                                    <TextBlock x:Uid="LlmProfile_ContextBody"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,-4,0,0" />

                                    <!-- Advanced parameters -->
                                    <Expander x:Uid="LlmProfile_AdvancedExpander"
                                              HorizontalAlignment="Stretch"
                                              HorizontalContentAlignment="Stretch">
                                        <StackPanel Spacing="12">
                                            <NumberBox x:Uid="LlmProfile_TopP"
                                                       Minimum="0" Maximum="1"
                                                       SmallChange="0.05" LargeChange="0.1"
                                                       SpinButtonPlacementMode="Compact"
                                                       Value="{x:Bind TopP, Mode=TwoWay}"
                                                       MinWidth="160" />
                                            <NumberBox x:Uid="LlmProfile_RepeatPenalty"
                                                       Minimum="0.5" Maximum="2"
                                                       SmallChange="0.05" LargeChange="0.1"
                                                       SpinButtonPlacementMode="Compact"
                                                       Value="{x:Bind RepeatPenalty, Mode=TwoWay}"
                                                       MinWidth="160" />
                                        </StackPanel>
                                    </Expander>
                                </StackPanel>
                            </controls:SettingsCard>
                        </controls:SettingsExpander.Items>
                    </controls:SettingsExpander>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <Button x:Uid="LlmAddProfileButton"
                Click="AddProfile_Click"
                Margin="0,4,0,0" />
    </StackPanel>
</UserControl>
