<Page
    x:Class="Deckle.Llm.Rewrite.LlmPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:llm="using:Deckle.Llm.Rewrite">

    <Page.Resources>
        <llm:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Page.Resources>

    <Grid Background="Transparent"
          Tapped="OnBackgroundTapped">
    <ScrollViewer Padding="32,24,32,24"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Background="Transparent">
            <StackPanel MaxWidth="1000"
                        HorizontalAlignment="Stretch"
                        Spacing="{StaticResource SettingsCardSpacing}">

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Uid="LlmPageTitleLabel"
                               Style="{StaticResource TitleTextBlockStyle}" />
                    <Button x:Uid="LlmPage_ResetAll"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Style="{StaticResource SubtleButtonStyle}"
                            Click="ResetAll_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="{StaticResource Icon.Reset}" FontSize="14" />
                            <TextBlock x:Uid="LlmPageResetAllLabel" />
                        </StackPanel>
                    </Button>
                </Grid>

                <TextBlock x:Uid="LlmPageSubtitleLabel"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12" />

                <InfoBar x:Name="OllamaStatusBar"
                         x:Uid="LlmPage_OllamaStatusBar"
                         Severity="Warning"
                         IsOpen="False"
                         IsClosable="False"
                         Margin="0,0,0,8" />

                <llm:LlmGeneralSection x:Name="GeneralSection" />

                <!-- Mask-not-grey: the four dependent sections collapse out of
                     layout when rewriting is off, driven by the General master
                     toggle. The Enabled toggle itself (in GeneralSection) stays
                     always visible. -->
                <llm:LlmShortcutSlotsSection x:Name="ShortcutSlotsSection"
                                             Visibility="{x:Bind GeneralSection.IsRewritingEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />
                <llm:LlmRulesSection x:Name="RulesSection"
                                     Visibility="{x:Bind GeneralSection.IsRewritingEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />
                <llm:LlmProfilesSection x:Name="ProfilesSection"
                                        Visibility="{x:Bind GeneralSection.IsRewritingEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />
                <llm:LlmModelsSection x:Name="ModelsSection"
                                      Visibility="{x:Bind GeneralSection.IsRewritingEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

            </StackPanel>
        </Grid>
    </ScrollViewer>
    </Grid>
</Page>
