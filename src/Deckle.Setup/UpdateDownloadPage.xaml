<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Deckle.Setup.UpdateDownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="16" MaxWidth="640" HorizontalAlignment="Stretch"
                VerticalAlignment="Center">

        <!-- Marquee while resolving the sidecar and extracting; determinate
             with real byte counts during the payload download. -->
        <ProgressBar x:Name="DownloadProgress" IsIndeterminate="True" />

        <TextBlock x:Name="StatusText"
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center" />

        <!-- A failed fetch/verify/extract lands here with Retry as the
             InfoBar's action — same blocking-state shape as DeployPage. -->
        <InfoBar x:Name="ErrorBar"
                 IsOpen="False"
                 IsClosable="False"
                 Severity="Error">
            <InfoBar.ActionButton>
                <Button x:Name="RetryButton" Click="OnRetryClick" />
            </InfoBar.ActionButton>
        </InfoBar>

    </StackPanel>
</Page>
