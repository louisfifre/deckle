<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Deckle.Setup.DeployPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="16" MaxWidth="640" HorizontalAlignment="Stretch"
                VerticalAlignment="Center">

        <!-- Local copy + registry integration: seconds of work with no byte
             counts worth a determinate bar — marquee until handoff. -->
        <ProgressBar x:Name="DeployProgress" IsIndeterminate="True" />

        <TextBlock x:Name="StatusText"
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center" />

        <!-- Blocking states (Deckle already running, a failed step) land here
             with Retry as the InfoBar's action. -->
        <InfoBar x:Name="ErrorBar"
                 IsOpen="False"
                 IsClosable="False"
                 Severity="Error">
            <InfoBar.ActionButton>
                <Button x:Name="RetryButton" Click="OnRetryClick" />
            </InfoBar.ActionButton>
        </InfoBar>

    </StackPanel>
</Page>
