<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Deckle.Setup.RelocatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="16" MaxWidth="640" HorizontalAlignment="Stretch"
                VerticalAlignment="Center">

        <!-- Marquee during the space recheck and the switch; determinate
             with real byte counts while the tree copies. -->
        <ProgressBar x:Name="RelocateProgress" IsIndeterminate="True" />

        <TextBlock x:Name="StatusText"
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center" />

        <!-- A failed check or copy lands here with Retry as the InfoBar's
             action — same blocking-state shape as DeployPage. -->
        <InfoBar x:Name="ErrorBar"
                 IsOpen="False"
                 IsClosable="False"
                 Severity="Error">
            <InfoBar.ActionButton>
                <Button x:Name="RetryButton" Click="OnRetryClick" />
            </InfoBar.ActionButton>
        </InfoBar>

    </StackPanel>
</Page>
