<Window
    x:Class="Deckle.App.LogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:Deckle.App"
    xmlns:logging="using:Deckle.App.Logging"
    Title="Deckle Logs">

    <Grid x:Name="RootGrid">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.ThemeDictionaries>
                    <!-- Semantic Fluent blue for Info — not the system accent
                         which may be grey / non-blue on the user's machine. -->
                    <ResourceDictionary x:Key="Light">
                        <SolidColorBrush x:Key="LogInfoBrush" Color="#005FB7" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Dark">
                        <SolidColorBrush x:Key="LogInfoBrush" Color="#60CDFF" />
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>

                <!-- 6 templates per wrap dimension, swapped via DataTemplateSelector
                     on Wrap toggle. Each template binds its Foreground to a
                     ThemeResource: WinUI re-resolves automatically on theme
                     switch, no need to rebuild entries in code.
                     Narrative uses TextFillColorPrimaryBrush (full strength
                     body text) so the user-facing pipeline narration reads
                     as primary content, not as a colored log level. -->
                <DataTemplate x:Key="NoWrap_Verbose">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Info">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource LogInfoBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Success">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Warning">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorCautionBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Error">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Narrative">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>

                <DataTemplate x:Key="Wrap_Verbose">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Info">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource LogInfoBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Success">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Warning">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorCautionBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Error">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Narrative">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>

                <!-- Telemetry (latency / corpus) rows: rendered in secondary text
                     color so they read as diagnostic metadata, not as log lines.
                     Text is pre-formatted by TelemetryEvent.Text (compact form). -->
                <DataTemplate x:Key="NoWrap_Latency">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="NoWrap_Corpus">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="NoWrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Latency">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>
                <DataTemplate x:Key="Wrap_Corpus">
                    <TextBlock Text="{Binding Text}"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               FontFamily="Consolas" FontSize="12"
                               TextWrapping="Wrap" />
                </DataTemplate>

                <logging:TelemetryEventTemplateSelector x:Key="NoWrapSelector"
                    Verbose="{StaticResource NoWrap_Verbose}"
                    Info="{StaticResource NoWrap_Info}"
                    Success="{StaticResource NoWrap_Success}"
                    Warning="{StaticResource NoWrap_Warning}"
                    Error="{StaticResource NoWrap_Error}"
                    Narrative="{StaticResource NoWrap_Narrative}"
                    Latency="{StaticResource NoWrap_Latency}"
                    Corpus="{StaticResource NoWrap_Corpus}" />
                <logging:TelemetryEventTemplateSelector x:Key="WrapSelector"
                    Verbose="{StaticResource Wrap_Verbose}"
                    Info="{StaticResource Wrap_Info}"
                    Success="{StaticResource Wrap_Success}"
                    Warning="{StaticResource Wrap_Warning}"
                    Error="{StaticResource Wrap_Error}"
                    Narrative="{StaticResource Wrap_Narrative}"
                    Latency="{StaticResource Wrap_Latency}"
                    Corpus="{StaticResource Wrap_Corpus}" />

                <!-- Compact container for dense log rendering: reduced padding,
                     hover highlight preserved (signals click-to-copy). -->
                <Style x:Key="LogItemContainerStyle" TargetType="ListViewItem">
                    <Setter Property="MinHeight" Value="0" />
                    <Setter Property="Padding" Value="4,1" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ── Native title bar (Windows App SDK 1.6+) ────────────────────── -->
        <!-- The TitleBar control handles drag region, themed caption buttons,
             height, and exposes a Content slot where we place the SearchBox. -->
        <TitleBar Grid.Row="0"
                  x:Name="AppTitleBar"
                  x:Uid="LogWindowTitleBar">
            <TitleBar.IconSource>
                <ImageIconSource x:Name="AppTitleBarIcon" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <!-- Fixed-width search, same pattern as WinUI 3 Gallery. Below
                     the narrow threshold (see code-behind), SearchBox collapses
                     and SearchIconButton takes over; clicking the icon reveals
                     the search, focus leaving it restores the icon. -->
                <Grid HorizontalAlignment="Right"
                      VerticalAlignment="Center"
                      Margin="8,0">
                    <AutoSuggestBox x:Name="SearchBox"
                                    x:Uid="LogWindowSearchBox"
                                    QueryIcon="Find"
                                    Width="320"
                                    VerticalAlignment="Center"
                                    TextChanged="OnSearchTextChanged"
                                    LostFocus="OnSearchBoxLostFocus" />
                    <Button x:Name="SearchIconButton"
                            x:Uid="LogWindowSearchIconButton"
                            Style="{StaticResource SubtleButtonStyle}"
                            Visibility="Collapsed"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            MinWidth="36"
                            Padding="8,4"
                            Click="OnSearchIconClick">
                        <FontIcon Glyph="{StaticResource Icon.Search}" FontSize="16" />
                    </Button>
                </Grid>
            </TitleBar.Content>
        </TitleBar>

        <!-- ── Command bar: level selector + actions ─────────────────────── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:SelectorBar Grid.Column="0"
                                  x:Name="LevelSelector"
                                  Margin="4,0,0,0"
                                  VerticalAlignment="Center"
                                  SelectionChanged="OnLevelSelectorChanged">
                <controls:SelectorBarItem x:Name="LevelFull" x:Uid="LogWindowLevelFull" />
                <controls:SelectorBarItem x:Name="LevelFiltered" x:Uid="LogWindowLevelFiltered" />
                <controls:SelectorBarItem x:Name="LevelCritical" x:Uid="LogWindowLevelCritical" />
            </controls:SelectorBar>

            <!-- Native overflow via IsDynamicOverflowEnabled (true by default)
                 + DynamicOverflowOrder. Higher = migrates first.
                 Group 2 (Copy/Save/Clear/Sep) goes first, then group 1
                 (AutoScroll/Wrap). The CommandBar handles measure, popup
                 and placement itself. -->
            <CommandBar Grid.Column="1"
                        x:Name="MainCommandBar"
                        DefaultLabelPosition="Right"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        IsOpen="False">
                <AppBarButton x:Name="CopyButton" x:Uid="LogWindowCopyButton"
                              Click="OnCopyClick">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="{StaticResource Icon.Copy}" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="SaveButton" x:Uid="LogWindowSaveButton"
                              Click="OnSaveClick">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="{StaticResource Icon.Save}" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="ClearButton" x:Uid="LogWindowClearButton"
                              Click="OnClearClick">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="{StaticResource Icon.Delete}" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <CommandBar.SecondaryCommands>
                    <AppBarToggleButton x:Name="AutoScrollToggle"
                                        x:Uid="LogWindowAutoScrollToggle"
                                        IsChecked="True">
                        <AppBarToggleButton.Icon>
                            <FontIcon Glyph="{StaticResource Icon.Export}" />
                        </AppBarToggleButton.Icon>
                    </AppBarToggleButton>
                    <AppBarToggleButton x:Name="WrapToggle"
                                        x:Uid="LogWindowWrapToggle"
                                        IsChecked="False"
                                        Click="OnWrapToggleClick">
                        <AppBarToggleButton.Icon>
                            <FontIcon Glyph="{StaticResource Icon.Close}" />
                        </AppBarToggleButton.Icon>
                    </AppBarToggleButton>
                </CommandBar.SecondaryCommands>
            </CommandBar>
        </Grid>

        <!-- ── Log surface ────────────────────────────────────────────────── -->
        <!-- Native ListView: virtualization, built-in scroll, ItemTemplateSelector
             honored. Left click = copy line, right click = native MenuFlyout.
             Floating "Copy" badge positioned to the right of the hovered item. -->
        <Grid Grid.Row="2" x:Name="LogSurface">
            <!-- Bottom padding 24 keeps the last line above the horizontal
                 scrollbar (which floats over the content in WinUI 3). Without
                 it, when scrolled to the bottom, the freshest line is masked
                 by the scrollbar — and the bottom is exactly where new entries
                 land. -->
            <ListView x:Name="LogItems"
                      Background="{ThemeResource LayerFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                      BorderThickness="0,1,0,0"
                      Padding="12,4,12,24"
                      ItemContainerStyle="{StaticResource LogItemContainerStyle}"
                      ItemTemplateSelector="{StaticResource NoWrapSelector}"
                      SelectionMode="Extended"
                      IsItemClickEnabled="False"
                      PointerMoved="OnLogPointerMoved"
                      PointerExited="OnLogPointerExited"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto" />

            <!-- Floating "Copy" / "Copy selection" badge — positioned by code
                 to the right of the hovered item. Fully opaque background
                 (SolidBackgroundFillBase) to completely mask the text and
                 selection highlight underneath. IsHitTestVisible=False to
                 let clicks pass through to the ListView. -->
            <Border x:Name="CopyBadge"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Visibility="Collapsed"
                    IsHitTestVisible="False"
                    Canvas.ZIndex="1"
                    Padding="8,2"
                    Margin="0,0,18,0"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="CopyBadgeTransform" />
                </Border.RenderTransform>
                <TextBlock x:Name="CopyBadgeText"
                           x:Uid="LogWindowCopyBadgeText"
                           FontSize="11"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Border>
        </Grid>
    </Grid>
</Window>
