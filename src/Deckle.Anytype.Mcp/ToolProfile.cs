namespace Deckle.Anytype.Mcp;

// The reusable Anytype tool profiles McpToolset can build. Custom MCP adapters
// expose McpSurface directly and do not extend this enum.
public enum ToolProfile
{
    ProjectManagement,
    Dialogues,
    SchemaAdmin,
    All,
}
