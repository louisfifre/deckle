<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Deckle.Vision</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Versions aligned on Deckle.csproj. WindowsAppSDK is required for
         the CsWinRT projection of IDirect3DDevice (the WinRT wrapper
         FrameSampler holds onto for D3D11 texture allocation). The
         capture pipeline itself uses DXGI Output Duplication via raw
         COM interop (no WinRT capture API anymore — see
         ScreenCaptureService class header for the migration rationale).
         The library is not a WinUI XAML lib — UseWinUI stays off because
         Deckle.Vision consumes the headless WinRT APIs only. -->
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.8.260317003" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.4654" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Deckle.Core\Deckle.Core.csproj" />
    <ProjectReference Include="..\Deckle.Diagnostics\Deckle.Diagnostics.csproj" />
    <!-- Deckle.Composition provides ColorSpace.ScRgbToSrgb (HDR
         tone-mapping) consumed by FrameSampler. -->
    <ProjectReference Include="..\Deckle.Composition\Deckle.Composition.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <!-- EventSource [Event] parameters use snake_case so the ETW
         payload keys land directly in the JSONL output. Convention
         documented in ../Deckle.Diagnostics/CLAUDE.md. -->
    <NoWarn>$(NoWarn);IDE1006</NoWarn>
  </PropertyGroup>
</Project>
